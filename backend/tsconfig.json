{
  "compilerOptions": {
    "target": "es6", // Specifies the ECMAScript version to compile to. "es6" is a common choice for modern JavaScript features.
    "module": "commonjs", // Specifies the module system to use. "commonjs" is used for Node.js environments.
    "outDir": "./dist", // Specifies the output directory for compiled JavaScript files.
    "strict": true, // Enables strict type checking and additional TypeScript features for better code quality.
    "esModuleInterop": true, // Enables interoperability between CommonJS and ES Modules.
    "moduleResolution": "node" // Specifies how module imports are resolved. "node" uses Node.js resolution algorithm.
  },
  "include": ["./**/*.ts"] // Specifies the files to include in the compilation process. This includes all TypeScript files in the "src" directory and its subdirectories.
}
